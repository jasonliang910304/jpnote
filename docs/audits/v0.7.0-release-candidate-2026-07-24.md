# jpnote v0.7.0 Release-Candidate Health Check

Date: 2026-07-24 (Asia/Taipei)

## Candidate

- source version: `0.7.0`
- formal release/tag before final gate: `v0.6.6.4`
- core SQLite schema: `5`
- Quiz SQLite schema: `2`
- public import JSON schema: unchanged by Quiz

## Completed scope

- optional/fault-isolated Quiz package and stable core read contracts;
- independent Quiz session/history store;
- safe generators and mixed/vocabulary/mistake question pools;
- headless service and lifecycle recovery;
- Python-native curses TUI;
- official `jpnote quiz` lazy-loaded CLI and config;
- JLPT/source filters, recent history, resume, summaries, and per-question history;
- isolated install/reinstall/package/failure-isolation smoke.

## Validation

```text
python -m compileall -q jpnote_app tests    PASS
pytest -q                                  376 passed, 18 subtests passed
app-only coverage                          76% (8308 statements, 1986 missed)
bash -n install.sh                          PASS
isolated installed smoke                   PASS
Quiz lazy loader                           PASS
fresh isolated init/stats                  PASS
tracked secret/database guard              PASS
git diff --check                           PASS
release-readiness audit                    PASS
```

## Safety findings

- core initialization does not create `quiz.db`;
- Quiz history does not write to core `attempts`;
- missing Quiz package fails cleanly without breaking core CLI;
- no tracked database, environment secret, private key, or credential-like file was detected;
- core schema remains v5 and no core migration is introduced by v0.7.0;
- normal grammar/vocabulary imports may continue until the short real-install smoke window.

## Remaining release gate

1. use a copy of the real core database for quick/foreign-key/audit/stats/read-only Quiz planning;
2. run an isolated v0.6.6.4 → v0.7.0 upgrade smoke;
3. pause imports, create a current backup, and run the real installer/installed-command smoke;
4. confirm installed data integrity;
5. create the release commit, annotated `v0.7.0` tag, patch, SHA-256, and final continuation prompt.

## Deferred, non-blocking

- TUI buttons for history export/delete;
- optional negative scoring / guess penalty;
- per-question response timing;
- spaced repetition, familiarity, streaks, radar charts, and long-term trends.

## Gate

`RELEASE CANDIDATE READY FOR DB-COPY / UPGRADE / REAL-INSTALL VALIDATION`
