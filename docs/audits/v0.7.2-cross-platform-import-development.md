# v0.7.2 cross-platform import release audit

日期：2026-08-08（Asia/Taipei）
基線：clean `e5ac8a800061c950f25586b28dd2c8c51c0d7850`（v0.7.1）
狀態：release gate 完成；v0.7.2 release commit 為 `46644a1ea329d15c85f35b897485763278aa0787`；本文件後續由 post-release CI maintenance／handoff sync 更新

## Scope

- `jpnote import --stdin`／`jpnote import -`
- versioned `jpnote.import.v1` JSON protocol
- preflight/apply token binding
- repository-owned Windows PowerShell 5.1／7 client
- versioned Windows install/update/uninstall
- Windows CI

本版不修改 core SQLite schema v5、Quiz schema v2 或 public import JSON schema。

## Threat／failure preflight

### Transport and parser

- locale-dependent decoding：core 直接讀 stdin bytes，以 strict `utf-8-sig` 解碼。
- unbounded stdin／file allocation：core 與 Windows client 都限制 16 MiB。
- mixed human/machine output：protocol success/error 只在 stdout 回傳單一 ASCII-safe JSON envelope；human warnings 留在 stderr。
- protocol drift：client 驗證 protocol id/version/mode/ok，無法解析或輸出不明時 fail closed。

### Stale approval and concurrent writers

- Windows precheck 使用與 apply 相同的 `--yes` safe-fix 模擬。
- check 對 normalized plan＋preflight report 產生 SHA-256 `preflight_token`。
- apply 帶回 token；payload、resolution 或相關 DB outcome 改變時立即拒絕。
- token 通過後，v0.7.1 writer lock 內仍會重新建立 DB-dependent warnings/preflight；兩層檢查皆不可省略。

### Windows local file

- strict UTF-8／JSON syntax validation。
- 拒絕 directory 與 reparse point。
- snapshot 讀取期間比對 size、creation time、last-write time。
- 正式 apply 前再次比對 SHA-256；成功後刪除前再比一次。
- remote import 未明確成功、token 不一致、review/conflict、source drift 或 delete error 都不刪 Windows source。
- source cleanup 是 post-commit fail-soft；刪檔失敗不回滾已成功 DB transaction。

### SSH and command construction

- host／alias 僅接受安全字元；其他 remote arguments 固定，token 僅接受 64 位 lowercase hex。
- 原始 JSON bytes 寫入 `ssh.exe` stdin，不使用 PowerShell text pipeline、clipboard、Base64、shell interpolation 或 remote temp file。
- stderr 不 redirect，讓 OpenSSH passphrase prompt 保持連到 Windows console；此行為已由 Windows PowerShell 5.1＋真實 SSH 實機確認。

### Windows installation

- PowerShell module 使用 versioned directory 與 manifest。
- installer 預設同時安裝 Windows PowerShell 5.1／PowerShell 7 user module path。
- same-version reinstall 先移至 `_backups`；新安裝驗證失敗時回復舊 target。
- existing reparse-point install target／legacy file fail closed。
- uninstall 只移除精確 0.7.2 target，拒絕 reparse point，不碰 backup 或其他版本。

## Local verification completed

```text
python -m py_compile jpnote_app/*.py                    PASS
pytest -q tests/test_v072_import_protocol.py
          tests/test_v072_windows_client_contract.py   20 passed
source CLI version                                     jpnote 0.7.2
install.sh syntax                                      PASS
protocol valid check/import/no-op                      PASS
protocol invalid UTF-8/JSON                            PASS
preflight token success/stale rejection                PASS
protocol stdout ASCII-only                             PASS
legacy --format json remains unwrapped                 PASS
PowerShell files UTF-8 BOM                             PASS
static no-Base64/no-mktemp contract                     PASS
```

Windows PowerShell 5.1＋real SSH/passphrase/client import gate 已由實機完成，且後續兩日使用正常。GitHub Actions 仍保留 Windows PowerShell 5.1／PowerShell 7 module/install/reinstall/uninstall regression。

### Post-release Windows CI maintenance

- release commit `46644a1ea329d15c85f35b897485763278aa0787` push 後，首次 workflow run 在 job 啟動前 validation failure。
- GitHub 回報 `steps[*].shell` 的 `${{ matrix.shell }}` 無法使用 `matrix` context；因此不是 client test failure。
- `694b869` 將原本 shell matrix job 改為兩個明確 job：`shell: powershell` 與 `shell: pwsh`，維持 native contract 與 install/reinstall/uninstall coverage。
- `e0095ba` 更新 repository contract test，移除對舊 matrix workflow 的過期 assertion。
- `28ba7ef` 將依賴 POSIX `fcntl` 的 core/import protocol job 從 Windows runner 移到 `ubuntu-latest`；Windows runner 只負責 PowerShell client jobs。
- 最終 GitHub Actions：`protocol` PASS、Windows PowerShell 5.1 PASS、PowerShell 7 PASS。
- v0.7.2 annotated tag 保持指向原 release commit `46644a1ea329d15c85f35b897485763278aa0787`，不因 CI-only maintenance 移動。

## Release completion

```text
actual repository full regression       421 passed, 18 subtests passed
versioned isolated install              PASS
stdin protocol／DB smoke                PASS
formal installed version                jpnote 0.7.2
formal DB backup                         pre-v0.7.2-dev1-20260803T224018.db
formal DB install before/after SHA-256   d288c557e455d7242d487c2b2f4dd3f49f0ea0073e720bb2c7f16b34649ddbac
formal DB gate entries／attempts          486／27
formal copy critical／needs_input         0／0
Windows PowerShell 5.1＋SSH real smoke   PASS
subsequent two-day Windows use           PASS
```

Release 當下 `HEAD`、`origin/main` 與 annotated `v0.7.2^{}` 已一致指向 `46644a1ea329d15c85f35b897485763278aa0787`。其後 main 僅以前述 CI-only maintenance 前進，release tag 固定不動；最終 CI 已全綠。下一個功能性工作從 bounded safety/stability gate 開始。
